---
title: "Programação no R - Aula 9"
institute: "Prof. Maurício Garcia de Camargo. IO-FURG."
author: "Disciplina: Lógica da programação (de computadores) e análise de dados no R"
date: 2025-11-13
lang: pt-br
format: 
  revealjs:
#    theme: [serif, MeuCustom.scss]
    logo: images/furg3.png
    slide-number: true
    chalkboard: true #Muito bom para aula
    show-slide-number: all
    controls: true #Não é menu. ToDo.
    #Sem estes css o :::box ou info-box não funciona
    css: [assets/syntax-highlight.css, assets/sydney.css, assets/custom.css, assets/sydney-fonts.css]
---

## <span style="font-size:34pt">Funções vetorizadas</span>

Vocês aprenderam a fazer na unha a transformação de um vetor de abundância em vetor de presência-ausência.

```{r echo=TRUE}
pres_aus = c()  # cria vetor vazio

x = c(23, 0, 5, 0, 12)

for (i in 1:length(x)) {
  if (x[i] > 0) 
    pres_aus[i] = 1
   else 
    pres_aus[i] = 0
}

pres_aus
```

## <span style="font-size:34pt">Funções vetorizadas</span>

Isso pode ser feito de maneira muito mais simples com a função vetorizada `ifelse`.

```{r echo=TRUE}
x = c(23, 0, 5, 0, 12)
pres_aus = ifelse(x > 0, 1, 0)
pres_aus
```

Uma **função vetorizada** avalia todas as posições de um vetor lógico de uma só vez e devolve um vetor do mesmo comprimento, escolhendo elemento a elemento entre os valores de ***Verdadeiro*** ou ***falso***.  

## <span style="font-size:34pt">Funções vetorizadas</span>
**Outro exemplo:**
```{r, echo=TRUE}
x = c(-2, 0, 3, 0)  
ifelse(x == 0, "sim", "não")  
```

<br>

:::{.fragment}  
`ifelse(x == 0, "sim", "não")`  
deve ser lido como: para cada valor do vetor `x`, se for igual a zero substitui por "sim", caso contrário, por "não".
:::

## <span style="font-size:34pt">Exercício. Funções vetorizadas</span>

- Baixe a planilha `Plan1.xlsx` da página da disciplina.  
- Abra o arquivo e armazene no dataframe chamado de `dat`.  
- Crie um novo dataframe para o registro de presença-ausência das três espécies usando `ifelse`. 

## <span style="font-size:34pt">Rever aggregate() da aula passada</span>

## <span style="font-size:34pt">Breve aula sobre SQL</span>

- ***SQL = Structured Query Language***

:::{.fragment}  
- Nasceu na IBM, em 1970.
:::

:::{.fragment}  
- A ideia é guardar informações em tabelas (linhas e colunas) e depois relacioná-las através de uma linguagem parecida com o inglês.
:::

:::{.fragment}  
- SQL virou padrão universal como linguagem de banco de dados.
:::

## <span style="font-size:34pt">Breve aula sobre SQL</span>

- ***SQL é capaz de fazer tudo com banco de dados***

:::{.fragment}  
- Criar estruturas de dados: criar, alterar e apagar bancos, tabelas, índices.
:::

:::{.fragment}  
- Consultar dados: o clássico SELECT ... FROM ... WHERE ....
:::

:::{.fragment}  
- Inserir, atualizar e excluir dados (INSERT, UPDATE, DELETE).
:::

:::{.fragment}  
- Criar relatórios e análises: agregações (SUM, AVG, COUNT), agrupamentos (GROUP BY), filtros, junções entre tabelas (JOIN).
:::

## <span style="font-size:34pt">Breve aula sobre SQL</span>

Vamos começar criando um dataframe rudimentar chamado `tab`. 

:::{.fragment}  
```{r echo=TRUE}
set.seed(12)
col1 = c(rep('A',4),rep('B',4))
col2 = trunc(runif(8,0,4))
col3 = rnorm(8,10,4)
tab = data.frame(col1,col2,col3)
tab
```
:::

## <span style="font-size:34pt">Breve aula sobre SQL</span>

A função sqldf() cria um novo dataframe baseado na seleção feita pela linguagem SQL, que é muito simples.  
Exemplos:

```{r echo=TRUE}
library(sqldf)
# Seleciona todas as colunas (*) e todas as linhas
sqldf('SELECT * FROM tab') 
```

## <span style="font-size:34pt">Breve aula sobre SQL</span>
```{r echo=TRUE}
# Seleciona apenas a primeira colunas e todas as linhas
sqldf('SELECT col1 FROM tab') 
```

:::{.fragment}  
```{r echo=TRUE}
# Seleciona as duas primeira colunas e todas as linhas
sqldf('SELECT col1,col2 FROM tab') 
```
:::


## <span style="font-size:34pt">Breve aula sobre SQL</span>
```{r echo=TRUE}
# Filtro para as linhas (usando where)
sqldf('SELECT * FROM tab WHERE col1="A" ') 
```

:::{.fragment}  
```{r echo=TRUE}
# Filtro para as linhas com operador booleano (and / or)
sqldf('SELECT * FROM tab WHERE col1="A" and col2=3')
sqldf('SELECT * FROM tab WHERE col1="A" or col2=3') 
```
:::

## <span style="font-size:34pt">Breve aula sobre SQL</span>
```{r echo=TRUE}
# Cálculos de médias (avg), soma (sum), contagem (count) etc.
sqldf('SELECT AVG(col3) FROM tab') 
```

:::{.fragment}  
```{r echo=TRUE}
# Cálculos por grupos (usando group by)
sqldf('SELECT AVG(col3) FROM tab GROUP BY col1') 
```
:::

:::{.fragment}  
```{r echo=TRUE}
# Adicionando col1
sqldf('SELECT col1, AVG(col3) FROM tab GROUP BY col1') 
```
:::

## <span style="font-size:34pt">Resumo das sentenças SQL desta aula</span>
SELECT * FROM tab  
SELECT col1 FROM tab  
SELECT col1,col2 FROM tab  
SELECT * FROM tab WHERE col1="A"   
SELECT * FROM tab WHERE col1="A" and col2=3  
SELECT * FROM tab WHERE col1="A" or col2=3  
SELECT AVG(col3) FROM tab  
SELECT AVG(col3) FROM tab GROUP BY col1  
SELECT col1, AVG(col3) FROM tab GROUP BY col1  

## <span style="font-size:34pt">Breve aula sobre SQL</span>

A sentença abaixo cria mesma tabela que usamos com `aggregate()` na aula passada.  

<br>
`
sqldf('SELECT setor, estacao, AVG(abund) FROM dad GROUP BY setor,estacao')
`

<br>
Vejam o arquivo R de exercícios desta aula.
